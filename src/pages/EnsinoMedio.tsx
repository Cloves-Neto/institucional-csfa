import { SegmentPageTemplate, ensinoMedioSegment } from '@/features/academic';

export const EnsinoMedio = () => {
  return <SegmentPageTemplate segment={ensinoMedioSegment} />;
};

export default EnsinoMedio;
